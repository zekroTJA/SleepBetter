name: ${project.artifactId}
version: ${project.version}
main: de.zekro.sleepbetter.SleepBetter
api-version: 1.13
prefix: ${project.name}
authors: [zekro]
description: ${project.description}
website: ${project.url}
